/* Deployment configuration. DEFAULT_BRAND is the single line a rebrand edits (design.md §5). */

/* Active brand pack for this deployment. Overridable at boot only, via ?brand=<id>. */
window.DEFAULT_BRAND = 'bnc';

window.SITE = {
  repository: 'https://github.com/ThomasMorrierBompoint/financial-tools'
};

window.FEATURES = {};
